version: "3.4"

services:
  postgres:
    container_name: postgres
    image: postgres:10.5
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes: 
      - ./postgres-data:/var/lib/postgresql/data

volumes:
  postgres:
